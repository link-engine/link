export function onStart({ object, eventMap, input, textRenderer, scene }) {

}

export function onUpdate({ object, input, aliasTable, clock }) {


}


export function onCollide({object, other, sceneObjects, ENGINE}) {

    //ENGINE.removeObject(object);
}